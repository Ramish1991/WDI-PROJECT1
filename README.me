Create a Simon Says game that allows two players to play against eachother. Player with highest score wins. 

Create a function that allows computer to generate a random number. 
Give each cell with random number a class ("selected").
Set time out function that uses cells with class("selected") and attach the random number to them.

Create function that resets the board, allowing for players to make their move and switch turns. 
Set counter to 6, allowing 3 turns for each player. 
